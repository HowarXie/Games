import React from "react";
import "./header.css";

const Header = (): JSX.Element => {
    return (
        <header className="react-games-header">React Games</header>
    );
}

export default Header;